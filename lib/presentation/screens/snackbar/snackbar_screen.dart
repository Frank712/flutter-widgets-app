import 'package:flutter/material.dart';

class SnackbarScreen extends StatelessWidget {
  final name = 'snackbar_screen';
  const SnackbarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}